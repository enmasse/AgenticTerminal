namespace AgenticTerminal.UI;

public enum AgentPanelMode
{
    /// <summary>Agent panel is rendered as an inline pane inside the TUI.</summary>
    Embedded,

    /// <summary>Agent panel is rendered in a separate Avalonia window.</summary>
    Avalonia
}
